export interface Article {
    title: string;
    description: string;
    slug: string;
    body: string;
    author: string;
    tagList: string;
    favorited: string;
    favoritesCount: string;
    createdAt: string;
    updatedAt: string;
}
